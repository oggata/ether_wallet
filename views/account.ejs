<% include header %>

  <script language="javascript" type="text/javascript">
        function OnButtonClick() {
            var xhr = new XMLHttpRequest();
            //xhr.timeout=1000;
            var host = window.location.hostname;
            var port = window.location.port;
            var url = "http://" + host + ":" + port + "/api/user/create?name=aaa";
            xhr.open('GET', url);
            xhr.onload = function (e) {
                if (this.status == 200) {
                    var json = JSON.parse(this.responseText);
                    if(json.status == "logout"){
                        location.href='/login';
                        return;
                    }
                    if(json.status == "ok"){
                      //alert("Your address is「" + json.wallet_address + "」。Kye is「" + json.wallet_mnemonic + "」");
                      alert("Your address is「" + json.wallet_address + "」");
                      location.href='/';
                    }else{
                      alert(json.message);
                      location.href='/';
                    }
                }
            };
            xhr.send();
        }
  </script>

  <body>

    <link href="//netdna.bootstrapcdn.com/font-awesome/4.0.0/css/font-awesome.css" rel="stylesheet">
    <div class="container text-center">
        <h2 class="form-signin-heading">Create New Account</h2>
    <small class="text-muted">Create New Account<br></small>
    <p>
    <br>
    <input class="fa" type="button" value="Exec" onclick="OnButtonClick();"/><br />
    </p>



  </body>
</html>
